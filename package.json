{
  "name": "hostpanel",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "db:generate": "turbo run db:generate --filter=@hostpanel/db",
    "db:migrate": "turbo run db:migrate --filter=@hostpanel/db",
    "db:studio": "turbo run db:studio --filter=@hostpanel/db",
    "db:seed": "npm run db:seed -w @hostpanel/db",
    "test": "node ./scripts/smoke-test.mjs",
    "test:smoke": "node ./scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.3",
    "prettier": "^3.4.2",
    "@types/node": "^22.10.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "packageManager": "npm@10.9.2"
}
